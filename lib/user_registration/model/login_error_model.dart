class LoginError {
  int? code;
  Object? message;

  LoginError({
    this.code,
    this.message,
  });
}

class SignUpError {
  int? code;
  Object? message;

  SignUpError({
    this.code,
    this.message,
  });
}